// Inherit the parent event
event_inherited();

name = "Grand Stamina Potion"

for (var i = 0; i < array_length(colorMatch); i += 1) {
	colorReplace[i].red = colorMatch[i].green
	colorReplace[i].green = colorMatch[i].red
	colorReplace[i].blue = colorMatch[i].blue
	}